Resumo das alterações recentes
=============================

Commit: `cadc3e2` — Atualização automática pelo Copilot (push para `main`)

Alterações principais realizadas neste conjunto de mudanças:

- Atualizações de configuração Android:
  - `android/app/src/main/AndroidManifest.xml`
  - `android/gradle.properties`

- Configuração Supabase e integração:
  - `lib/config/supabase_config.dart`

- Entrypoint e telas principais:
  - `lib/main.dart`
  - `lib/telas/home_screen.dart`
  - `lib/telas/login_screen.dart`

- Model e repositório de usuário:
  - `lib/models/usuario.dart`
  - `lib/repositories/usuario_repository.dart`

- Ajustes em testes e arquivos gerados por plataforma:
  - `test/widget_test.dart`
  - `linux/flutter/generated_plugin_registrant.cc`
  - `linux/flutter/generated_plugins.cmake`
  - `macos/Flutter/GeneratedPluginRegistrant.swift`
  - `windows/flutter/generated_plugin_registrant.cc`
  - `windows/flutter/generated_plugins.cmake`

Observações:
- Arquivos gerados de plataforma foram atualizados (provavelmente pelo processo de build do Flutter).
- Linhas de fim de arquivo podem ser convertidas entre LF/CRLF no Windows ao tocar o Git.

Se quiser, posso criar uma tag ou release com este resumo, ou detalhar cada arquivo com o diff completo.
